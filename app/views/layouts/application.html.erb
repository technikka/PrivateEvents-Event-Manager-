<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="header">
      <div><%= link_to "Discover", root_path %></div>
      <% if user_signed_in? %>
        <div><%= link_to current_user.email, user_path(current_user.id) %></div>
        <div><%= link_to "Create", new_event_path %></div>
        <div><%= link_to "Sign Out", destroy_user_session_path %></div>
      <% else %>
      <div><%= link_to "Sign In", new_user_session_path %></div>
      <div><%= link_to "Sign Up", new_user_registration_path %></div>
      <% end %>
    </nav>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
